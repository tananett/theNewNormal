import 'package:flutter/material.dart';

var headerTextStyle =
    TextStyle(fontWeight: FontWeight.bold, fontSize: 20, letterSpacing: 0.5);

var normalTextStyle = TextStyle(fontSize: 17, letterSpacing: 0.5);
var fadeTextStyle = TextStyle(fontSize: 17, color: Colors.black54);
var shadowListTile = BoxShadow(
    color: Colors.black12,
    spreadRadius: 1.0,
    blurRadius: 6.0,
    offset: Offset(0, 4));
var shadowSearchBox = BoxShadow(
    color: Colors.black12,
    spreadRadius: 2.0,
    blurRadius: 3.0,
    offset: Offset(0, 4));
